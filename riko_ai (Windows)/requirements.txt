# requirements.txt
# Core dependencies (already installed)
groq

# Voice dependencies
pyttsx3>=2.90          # Text-to-Speech (offline, works on Linux/Windows/Mac)
SpeechRecognition>=3.10.0  # Speech-to-Text
pyaudio>=0.2.13        # Audio input for STT (requires system packages)

# GUI (GTK4) - install via system package manager
# Arch: sudo pacman -S gtk4 python-gobject
# Ubuntu: sudo apt install python3-gi gir1.2-gtk-4.0
